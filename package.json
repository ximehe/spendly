{
  "name": "spendly",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Spendly — registro financiero personal, en español, con formato regional de Uruguay.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  }
}
